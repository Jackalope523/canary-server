using System;

namespace Shared
{
    public enum UserRating
    { Positive, Negative, Remove }

    public enum EventUserState
    { Watching, Guest, Incoming, Arrived, Left, Kicked }

    public enum DeviceType
    { iOS, Android }
}
