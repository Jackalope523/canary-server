import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const rangeSelector = (props: Props) => {
  return (
    <View>
      <Text>rangeSelector</Text>
    </View>
  )
}

export default rangeSelector

const styles = StyleSheet.create({})