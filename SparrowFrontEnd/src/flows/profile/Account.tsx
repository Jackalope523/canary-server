import * as React from 'react';
import { View, Text } from 'react-native';

const AccountScreen = () => {
    return (
        <View>
            <Text>Account settings and whatnot</Text>
        </View>
    );
};

export default AccountScreen