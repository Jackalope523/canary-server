import { View, Text } from 'react-native'
import * as React from 'react'

type DiscoverySearchProps = {}

const DiscoverySearchScreen = (props: DiscoverySearchProps) => {
  return (
    <View>
      <Text>Discovery search screen</Text>
    </View>
  )
}

export default DiscoverySearchScreen